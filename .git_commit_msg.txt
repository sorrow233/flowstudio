feat: 写作模块 Markdown 语法全面支持与优化

用户反馈：
https://flowstudio.catzz.work/writing
MD 语法都不支持
支持的格式很少，甚至基础的

优化要求：
继续优化我给你的文案的功能和设计，找到你之前没有弄好的，进行全面的检查，重新设计，重写优化这次你增加的功能.确保第一版交给用户的就是一个能够使用和完善的功能。

变更内容：
1. 重构 Markdown 解析核心 (`markdownParser.js`)：
   - 实现完整块级元素解析（代码块、标题、引用、列表、分割线）。
   - 实现完整行内元素解析（粗体、斜体、删除线、行内代码、图片）。
   - 增加 XSS 防护，过滤脚本标签。
   - 使用 Tailwind CSS 实现高级排版样式。
2. 优化编辑器渲染逻辑：
   - 修复换行处理，使用 `<div>` 包裹段落。
   - 增强 `htmlToMarkup` 反向解析能力，支持从 HTML 还原 Markdown 结构。
3. 增强粘贴体验 (`pasteUtils.js`)：
   - 粘贴富文本时自动保留 Markdown 格式（标题、列表、代码块等）。
   - 直接复用 `htmlToMarkup` 逻辑，消除冗余代码。
4. 清理代码：
   - 删除未使用的 ProseMirror 相关文件。
5. 版本升级：
   - package.json 版本号升级至 2.6.0。
