---
description: 同步问题
---

1. Check git status to see if local branch is behind remote.
2. Run `git pull origin main` (or relevant branch).
3. Resolve any merge conflicts.
4. Ensure local dependencies are up to date with `npm install`.
